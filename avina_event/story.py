from memory.main import get_story_progress


def story_trigger():
    progress = get_story_progress()
    return
    if progress == 0:
        pass
        # speak("Opening scene. A sword, staff, and ball are stacked on a small hill.")
        # speak("A party sits around a campfire, looking sad.")
    else:
        # speak(f"Story progress: {get_story_progress()}")
        pass
